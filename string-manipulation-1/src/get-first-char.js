/* exported getFirstChar */

/*
retrieve the first character from argument
first character = first index (0)
return the first index of argument
*/

function getFirstChar(string) {
  return string[0];
}
