/* exported getValue */
/*
find the value of the given key(property) for an object
key is given as a string, need to use bracket notation to access value
*/

function getValue(object, key) {
  return object[key];
}
