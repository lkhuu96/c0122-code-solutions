/* exported last */
/*
need to return last element of array
last element is last index
last index is array length minus one
store lastindex value into variable
return the array at last index
*/

function last(array) {
  var lastIndex = array.length - 1;
  return array[lastIndex];
}
